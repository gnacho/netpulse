{
  "name": "netpulse-landing",
  "private": true,
  "description": "Static landing for NetPulse. build:langs prerenders one crawlable route per language (issue #842).",
  "type": "module",
  "scripts": {
    "build:langs": "node build-langs.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
